<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SOColeta.ViewModels"
             x:Class="SOColeta.Views.ConfigPage"
             x:DataType="vm:ConfigViewModel"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Servidor SMTP"/>
            <Frame>
                <Entry />
            </Frame>
            <Label Text="Endereço de email"/>
            <Frame>
                <Entry />
            </Frame>
            <Label Text="Senha"/>
            <Frame>
                <Entry IsPassword="True"/>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>