<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AppColeta.ViewModels"
             xmlns:model="clr-namespace:AppColeta.Models"
             x:Class="AppColeta.Views.ConfigPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Servidor SMTP"/>
            <Frame>
                <Entry />
            </Frame>
            <Label Text="Endereço de email"/>
            <Frame>
                <Entry />
            </Frame>
            <Label Text="Senha"/>
            <Frame>
                <Entry IsPassword="True"/>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>