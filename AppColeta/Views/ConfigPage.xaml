<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SOColeta.ViewModels"
             x:Class="SOColeta.Views.ConfigPage"
             x:DataType="vm:ConfigViewModel"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Padding="15">
            <Label Text="Comportamento ao deslizar"/>
            <StackLayout Orientation="Horizontal">
                <RadioButton Content="Executar" CheckedChanged="OnExecuteClicked" x:Name="rdExecute"/>
                <RadioButton Content="Revelar" CheckedChanged="OnRevealClicked" x:Name="rdReveal"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>