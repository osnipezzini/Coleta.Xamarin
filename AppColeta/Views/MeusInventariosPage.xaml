<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppColeta.Views.MeusInventariosPage"
             Title="Meus inventários">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Inventarios}" HasUnevenRows="True" IsPullToRefreshEnabled="True" RefreshCommand="{Binding LoadInventariosCommand}" 
                      IsRefreshing="{Binding IsBusy, Mode=TwoWay}" ItemSelected="ListView_ItemSelected" SeparatorColor="{StaticResource ElliteBlue}" Margin="0, 10, 0, 0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
                                <Label>
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Nome do arquivo" FontSize="20"/>
                                            <Span Text=" - "/>
                                            <Span Text="{Binding NomeArquivo}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label>
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Data de geração" FontSize="16"/>
                                            <Span Text=" - "/>
                                            <Span Text="{Binding DataCriacao, StringFormat='{0:dd/MM/yyyy}'}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
                </ListView>
                <!--<RefreshView x:DataType="local:MeusInventariosViewModel" Command="{Binding LoadInventariosCommand}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
                <CollectionView x:Name="ItemsListView"
                ItemsSource="{Binding Inventarios, Mode=TwoWay}"
                SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout Padding="10" x:DataType="model:Inventario">
                                <Label Text="{Binding NomeArquivo}" FontSize="16" Grid.Column="1"/>
                                <Label Text="{Binding DataCriacao}" FontSize="13" Grid.Row="1" Grid.Column="1"/>
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer 
                                NumberOfTapsRequired="1"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:MeusInventariosViewModel}}, Path=SelectedItem}"		
                                CommandParameter="{Binding .}">
                                    </TapGestureRecognizer>
                                </StackLayout.GestureRecognizers>
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </RefreshView>-->
            <Button Text="Exportar arquivo selecionado" Command="{Binding ExportFileCommand}"  FontSize="25" Margin="20, 20"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>