<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:framework="clr-namespace:SOFramework.Resources;assembly=SOFramework.Xamarin"
             x:Class="SOColeta.App">
    <!--
        Define global resources and styles here, that apply to all pages in your app.
    -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <framework:SOColors />
                <framework:SOShellStyle />
                <framework:SOAppStyle />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="NavigationPage" ApplyToDerivedTypes="True">
                <Setter Property="BarBackgroundColor" Value="{AppThemeBinding Dark={StaticResource SOHigh}, Light={StaticResource SOLow}}"/>
            </Style>
        </ResourceDictionary>        
    </Application.Resources>
</Application>
